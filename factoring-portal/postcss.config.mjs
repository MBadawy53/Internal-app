// Local PostCSS config (intentionally minimal). Our globals.css is hand-written
// plain CSS — no Tailwind. This file also stops PostCSS from searching parent
// directories for a config.
const config = { plugins: {} };
export default config;
